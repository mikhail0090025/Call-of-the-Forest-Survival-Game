using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DroppedItem : MonoBehaviour
{
    [SerializeField] TextOnWatching TextOnWatching;
    [SerializeField] CollectableObject CollectableObjectScript;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
